Download the latest version of MATLAB and place the project files into a designated folder. Subsequently, execute the `main.m` file. Upon successful code execution, the resulting plots will be displayed.

Here's a brief outline of the code's functionality:

*   **Project Setup:** The core of this project involves MATLAB scripts. Ensure MATLAB is installed and the project files are organized in a folder.
*   **Core Script:** The `main.m` file serves as the entry point, orchestrating the execution of other MATLAB functions.
*   **Expected Output:** After running `main.m`, you should see graphical results displayed in MATLAB's figure windows. These plots will present the outcomes of the Fringe Projection Profilometry (FPP) calculations.
The MATLAB scripts were developed with the assistance of Claude AI, a language model by Anthropic.
